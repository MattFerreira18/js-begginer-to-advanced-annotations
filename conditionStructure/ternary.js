// replace a sumple IF structure
// structure (condition) ? true value : false value

const scoreUser = 900;
const canVIPLevel = scoreUser >= 1000 ? 'VIP user' : 'normal user';

console.log(canVIPLevel);
